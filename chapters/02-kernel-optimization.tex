\section*{Kernel Optimization for Inference}
\addcontentsline{toc}{section}{2\quad Kernel Optimization for Inference}

\smallskip

\begin{whythis}
Chapter~1 established the memory wall as the governing constraint on inference hardware. This chapter makes it operational. The roofline model becomes a diagnostic tool applied to specific kernels --- attention, quantized matmuls, MoE routing --- with concrete numbers on concrete hardware. The question shifts from ``why is decode slow?'' to ``which kernel is the bottleneck, what regime is it in, and what optimization moves it to a faster regime?''

Three to five kernels dominate RLVR wall time. Each sits at a specific point on the roofline. Optimizing the wrong kernel --- or optimizing the right kernel for the wrong bottleneck --- wastes engineering weeks. The skill this chapter builds: diagnose first, optimize second, verify third. The tool: the roofline model, applied with numbers.
\end{whythis}

% =======================================================================
\subsection*{Move 1: The Roofline Model as Diagnostic Tool}
% =======================================================================

Chapter~1 introduced the roofline conceptually: operations are either compute-bound or memory-bound, and the ridge point separates the two regimes. Here we make it precise enough to use.

\textbf{The model.} Every kernel has an arithmetic intensity:
$$I = \frac{\text{FLOPs performed}}{\text{bytes moved between DRAM and on-chip memory}}$$

Every chip has a ridge point:
$$I^* = \frac{\text{peak compute (FLOP/s)}}{\text{peak bandwidth (B/s)}}$$

If $I < I^*$, the kernel is \textbf{memory-bound}: performance scales with bandwidth. If $I > I^*$, the kernel is \textbf{compute-bound}: performance scales with FLOP/s. The attainable throughput is:
$$\text{Throughput} = \min\left(\text{peak FLOP/s},\;\; I \times \text{peak bandwidth}\right)$$

This is a ceiling, not a guarantee. Real kernels hit 50--80\% of the roofline ceiling. The gap comes from imperfect tiling, bank conflicts, uncoalesced memory accesses, and pipeline bubbles. But the roofline tells you which ceiling you are hitting --- and therefore which class of optimization can help.

\textbf{Key insight: the same kernel, different regimes.} A batched matrix multiply at batch size 1 has arithmetic intensity $\approx 1$ FLOP/byte (BF16). At batch size 64, intensity rises to $\approx 32$ FLOP/byte. On an H100 with ridge point $\approx 148$ FLOP/byte, both are memory-bound. On a chip with ridge point $\approx 30$ FLOP/byte, batch-64 is compute-bound while batch-1 is memory-bound. The optimization strategy differs completely --- and you cannot know which applies without computing the numbers for your specific hardware.

\textbf{Reference hardware.} We use two chips throughout this chapter for concrete examples:

\begin{center}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
\textbf{Chip} & \textbf{BF16 Peak (TFLOP/s)} & \textbf{BW (TB/s)} & \textbf{Ridge (FLOP/byte)} \\
\midrule
H100 SXM (dense) & 495 & 3.35 & 148 \\
B200             & 4500 & 8.0 & 562 \\
\bottomrule
\end{tabular}
\end{center}

\begin{thinkfirst}[Autoregressive Decode Arithmetic Intensity]
For autoregressive decode with batch size 1, estimate the arithmetic intensity of a single transformer layer's attention computation. Assume head dimension $d = 128$, 32 heads, and context length $S = 2048$.

At each decode step, the query is a single vector of dimension $d$ per head. The KV-cache holds $S$ key vectors and $S$ value vectors per head, each of dimension $d$, in BF16. For one head:
\begin{itemize}
  \item $QK^\top$: multiply $[1 \times d]$ by $[d \times S]$ = $2dS$ FLOPs. Read $Q$ ($2d$ bytes) and $K$ cache ($2dS$ bytes).
  \item Softmax: $O(S)$ FLOPs, negligible relative to matmul.
  \item $\text{scores} \times V$: multiply $[1 \times S]$ by $[S \times d]$ = $2dS$ FLOPs. Read $V$ cache ($2dS$ bytes).
  \item Total per head: $4dS$ FLOPs, $\approx 4dS$ bytes read (dominated by KV-cache loads).
\end{itemize}

Arithmetic intensity per head $\approx 4dS / 4dS = 1$ FLOP/byte.

On a chip with 1000 TFLOP/s peak compute and 3 TB/s bandwidth, the ridge point is $\approx 333$ FLOP/byte. At intensity 1, this is deep in the memory-bound regime --- throughput is $1 \times 3 \times 10^{12} = 3$ TFLOP/s, using 0.3\% of peak compute.

This is why autoregressive decode is the bottleneck: the arithmetic units are idle $>99\%$ of the time, waiting for data. No amount of faster compute fixes this. Only bandwidth or data movement reduction helps.
\end{thinkfirst}

% =======================================================================
% KEY TIKZ VISUAL: Roofline diagram with inference kernels plotted
% =======================================================================

\begin{center}
\begin{tikzpicture}[
  scale=0.95,
  every node/.style={font=\footnotesize}
]
  % Axes (log-inspired but linear for clarity)
  \draw[-{Stealth[length=5pt]}, thick] (0,0) -- (10.5,0)
    node[right, font=\small] {Arithmetic Intensity (FLOP/byte)};
  \draw[-{Stealth[length=5pt]}, thick] (0,0) -- (0,6.5)
    node[above, rotate=90, anchor=south, yshift=3mm, font=\small] {Attainable TFLOP/s};

  % H100 roofline
  \draw[aiiro!70, very thick]
    (0,0) -- (4.2,4.2) -- (9.8,4.2);
  \node[aiiro!70, font=\scriptsize, anchor=west] at (9.9,4.2) {H100};
  \draw[aiiro!40, dashed, thin] (4.2,0) -- (4.2,4.2);
  \node[aiiro!70, font=\scriptsize, below] at (4.2,0) {148};

  % B200 roofline (higher ceiling)
  \draw[beni!60, very thick]
    (0,0) -- (6.2,6.2) -- (9.8,6.2);
  \node[beni!60, font=\scriptsize, anchor=west] at (9.9,6.2) {B200};
  \draw[beni!40, dashed, thin] (6.2,0) -- (6.2,6.2);
  \node[beni!60, font=\scriptsize, below] at (6.2,0) {562};

  % --- Kernel points ---

  % Autoregressive attention (batch=1): far left
  \fill[matcha!80!black] (0.3, 0.3) circle (3pt);
  \node[font=\scriptsize, matcha!70!black, anchor=south west, text width=2.2cm]
    at (0.4, 0.45) {AR decode\\(batch=1)};

  % Batched attention (batch=64)
  \fill[matcha!80!black] (1.6, 1.6) circle (3pt);
  \node[font=\scriptsize, matcha!70!black, anchor=south, text width=2cm, align=center]
    at (1.6, 1.8) {Batched attn\\(batch=64)};

  % Quantized decode (INT4): shifts right from batch=1
  \fill[kitsune!80!black] (0.9, 0.9) circle (3pt);
  \node[font=\scriptsize, kitsune!80!black, anchor=north west, text width=2cm]
    at (1.0, 0.75) {INT4 decode\\(batch=1)};

  % Prefill attention (long context): near/past H100 ridge
  \fill[aiiro!80!black] (4.8, 4.2) circle (3pt);
  \node[font=\scriptsize, aiiro!70!black, anchor=south west, text width=2cm]
    at (4.9, 4.3) {Prefill\\(long ctx)};

  % MoE forward pass: sparse utilization
  \fill[beni!80!black] (2.4, 1.5) circle (3pt);
  \node[font=\scriptsize, beni!70!black, anchor=south west, text width=2.5cm]
    at (2.5, 1.65) {MoE fwd\\(sparse util.)};

  % --- Annotations ---

  % Arrow showing quantization shift
  \draw[-{Stealth[length=4pt]}, kitsune!60, thick, dashed]
    (0.4, 0.35) -- (0.85, 0.85);
  \node[font=\tiny\itshape, kitsune!60, anchor=north, text width=1.5cm, align=center]
    at (0.55, 0.2) {quantize\\weights};

  % Arrow showing batching shift
  \draw[-{Stealth[length=4pt]}, matcha!60, thick, dashed]
    (0.45, 0.4) -- (1.45, 1.5);
  \node[font=\tiny\itshape, matcha!60, anchor=east, text width=1.3cm, align=center]
    at (0.7, 1.1) {increase\\batch};

  % Region label
  \node[font=\scriptsize\itshape, sumi!50, text width=2.5cm, align=center]
    at (1.0, 5.5) {memory-bound\\(optimize data\\movement)};
  \node[font=\scriptsize\itshape, sumi!50, text width=2.5cm, align=center]
    at (8.0, 5.5) {compute-bound\\(optimize\\arithmetic)};

  % Dividing hint
  \draw[sumi!20, thin, dashed] (4.2, 0) -- (4.2, 6.3);
\end{tikzpicture}
\end{center}

\smallskip
{\small\textit{Roofline diagram with inference kernels. Autoregressive decode sits deep in the memory-bound regime. Quantization and batching shift operations rightward along the x-axis. Prefill attention reaches the compute-bound regime on H100, but not on B200 (higher ridge point). MoE forward passes underperform their theoretical intensity due to sparse expert utilization and dispatch overhead.}}

\textbf{Reading the diagram.} Two operations produce the same measured TFLOP/s but for different reasons: AR decode at batch=1 hits the bandwidth ceiling at low intensity; prefill attention hits the compute ceiling at high intensity. The optimization for each is completely different. For decode, reduce bytes moved (quantization, KV-cache compression). For prefill, increase compute efficiency (larger tiles, better MXU/Tensor Core utilization). Applying the wrong optimization wastes engineering time.

\textbf{Quantization shifts the x-axis.} Moving weights from FP16 (2 bytes) to INT4 (0.5 bytes) reduces bytes loaded by $4\times$ without changing FLOPs. Arithmetic intensity quadruples. This is the primary mechanism by which quantization accelerates memory-bound kernels --- it is a bandwidth optimization, not a compute optimization. The FP4 compute throughput increase on Blackwell is a secondary benefit.

\textbf{Batching shifts the x-axis.} At batch size $B$, the weight matrix is loaded once and used for $B$ matrix-vector products. Arithmetic intensity scales linearly with $B$. This is why continuous batching (Chapter~3) matters: higher effective batch size moves decode closer to the ridge point.

% =======================================================================
\subsection*{Move 2: The Critical Kernels, Organized by Bottleneck}
% =======================================================================

The kernels that dominate RLVR inference wall time fall into three categories: attention kernels (the KV-cache problem), quantized matmuls (the weight loading problem), and MoE routing (the sparse dispatch problem). Each has a characteristic roofline position and a characteristic optimization strategy.

% -----------------------------------------------------------------------
\subsubsection*{2a: Attention Kernels}
% -----------------------------------------------------------------------

\textbf{Why naive attention is unacceptable.} Standard attention computes $O = \text{softmax}(QK^\top\!/\!\sqrt{d})\,V$. For sequence length~$N$ and head dimension~$d$, the intermediate score matrix $QK^\top$ is $N \times N$. Materializing it in DRAM costs $O(N^2)$ bytes. At $N{=}8192$ in FP16: 128\,MB per head, per layer. A 32-head, 32-layer model needs ${\sim}130$\,GB of intermediate storage --- exceeding a single H100's HBM.

The problem is not the FLOPs (attention is $O(N^2 d)$). The problem is materializing the $N^2$ matrix in DRAM, reading it back for softmax, then reading it again for the value projection. Three passes over $O(N^2)$ data when DRAM bandwidth is the bottleneck.

\textbf{FlashAttention: fuse everything, materialize nothing.} FlashAttention processes $Q$, $K$, $V$ in tiles that fit in SRAM. For each tile of queries, it streams through all key-value tiles, computing attention scores and accumulating the output --- without ever writing the full $N \times N$ matrix to DRAM.

The key algorithmic insight is the \textit{online softmax}: standard softmax requires a full pass to find the maximum (for numerical stability), then a second pass to exponentiate and normalize. The online algorithm maintains running statistics $(m, \ell)$ --- current maximum and current sum --- updating both as new tiles arrive. This enables single-pass softmax over blocks that never coexist in SRAM simultaneously.

HBM traffic drops from $\Theta(N^2 + Nd)$ (standard) to $\Theta(Nd)$ (FlashAttention) --- linear in sequence length. At $N = 8192$ and $d = 128$, this is a $64\times$ reduction in memory traffic. The compute is identical; only the data movement changes. This is a pure memory-hierarchy optimization.

\textbf{Decode vs.\ prefill: two regimes, one kernel.} The same attention operation sits in different roofline regimes depending on context:

\textit{Prefill} processes the full input sequence in parallel. $Q$, $K$, $V$ are all $[N \times d]$ matrices. The attention computation is a batched matmul with arithmetic intensity $O(N/d)$. At $N = 4096$ and $d = 128$, intensity $\approx 32$ FLOP/byte --- approaching compute-bound on some hardware.

\textit{Decode} generates one token at a time. $Q$ is $[1 \times d]$; the kernel reads the full KV-cache ($[S \times d]$ for each of $K$ and $V$, where $S$ is the current sequence length). This is a matrix-vector product: arithmetic intensity $\approx 1$ FLOP/byte, deep in the memory-bound regime regardless of hardware. Decode attention is the single most bandwidth-hungry operation in autoregressive generation.

The engineering consequence: decode attention benefits from bandwidth optimizations (KV-cache quantization, larger batch sizes). Prefill attention benefits from compute optimizations (better tiling, higher Tensor Core utilization). A single FlashAttention implementation must handle both regimes efficiently.

\textbf{Paged attention for KV-cache.} Naive KV-cache allocation reserves a contiguous memory block per sequence at the maximum context length. For RLVR rollouts with high length variance (100--4000 tokens), this wastes memory proportional to the gap between actual and maximum length.

Paged attention (vLLM) manages KV-cache as fixed-size blocks allocated on demand and mapped to non-contiguous physical memory --- analogous to OS virtual memory pages. Fragmentation drops from $O(S_{\max})$ to $O(\text{block size})$ per sequence. This directly increases the number of concurrent rollouts that fit in HBM, which increases effective batch size, which moves decode attention rightward on the roofline.

\textbf{KV-cache quantization.} FP8 or INT8 quantization of the KV-cache reduces memory by $2\times$ (from FP16) and bytes loaded per decode step by the same factor. The arithmetic intensity of decode attention doubles. For RLVR, this requires caution: KV-cache quantization introduces noise in attention scores, which propagates to generation quality, which directly affects the reward signal and gradient estimates. Unlike supervised training where a small quality degradation is tolerable, RL amplifies distributional shifts --- a 2\% quality drop may produce a disproportionate change in the reward distribution, leading to noisier advantage estimates and slower policy improvement. Measure reward variance, not just perplexity, before deploying KV-cache quantization in an RL pipeline.

\begin{exercise}[FlashAttention Roofline Profiling]
Profile FlashAttention decode latency across batch sizes 1, 8, 32, 64. For each batch size, compute:
\begin{itemize}
  \item Achieved HBM bandwidth (bytes loaded / latency).
  \item Arithmetic intensity (FLOPs / bytes loaded).
  \item Attainable TFLOP/s from the roofline model.
\end{itemize}

Plot all four points on the roofline diagram for your hardware. Identify the crossover batch size where decode transitions from memory-bound to compute-bound. On H100 (ridge $\approx 148$ FLOP/byte), expect this crossover to be unreachable at practical batch sizes --- verify.

Use a 7B model, sequence length 2048, FP16. Warm up 10 iterations, measure 100, report median. If achieved bandwidth is below 60\% of peak at batch size 1, profile memory access patterns --- non-coalesced KV-cache reads from paged attention can cause this.
\end{exercise}

% -----------------------------------------------------------------------
\subsubsection*{2b: Quantized Matmuls}
% -----------------------------------------------------------------------

During autoregressive decode, the weight matmul loads the full parameter matrix from DRAM at every step: for a 7B model in BF16, that is $\sim$14\,GB per step. At H100's 3.35\,TB/s, the minimum time per step is $14 / 3350 \approx 4.2$\,ms --- just from weight loading. This sets a hard floor on decode latency that no compute optimization can break.

\textbf{Weight-only quantization.} Reducing weight precision from FP16 to INT8 halves the bytes loaded: 7\,GB per step, minimum latency $\approx 2.1$\,ms. INT4 quarters it: 3.5\,GB, minimum $\approx 1.0$\,ms. The FLOPs are unchanged (dequantization to FP16/FP32 for the actual multiply-accumulate is negligible), but the bandwidth requirement halves or quarters. This is the mechanism: quantization is a bandwidth optimization for memory-bound kernels.

\textbf{Arithmetic intensity by precision.} For an $N \times N$ GEMM:

\begin{center}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Precision} & \textbf{Bytes/element} & \textbf{Data loaded} & \textbf{Intensity ($N/\ldots$)} & \textbf{H100 bound ($N=4096$)} \\
\midrule
FP16  & 2 & $6N^2$ bytes & $N/3$ & compute ($I = 1365$) \\
FP8   & 1 & $3N^2$ bytes & $2N/3$ & compute ($I = 2731$) \\
FP4   & 0.5 & $1.5N^2$ bytes & $4N/3$ & compute ($I = 5461$) \\
\bottomrule
\end{tabular}
\end{center}

\smallskip
For large square GEMMs (training), all precisions are compute-bound on H100. The benefit of lower precision is more FLOP/s from the Tensor Cores, not less bandwidth.

For decode (effective $N \approx \text{batch size} \times d$, typically $\ll 4096$): all precisions are memory-bound. The benefit of lower precision is fewer bytes loaded --- a bandwidth optimization.

\textbf{FP4 on Blackwell.} Blackwell's B200 offers $\sim$18,000 TFLOP/s at FP4 with 8\,TB/s bandwidth. Ridge point: $18000 / 8 = 2250$ FLOP/byte. For a decode-step weight matmul at batch size 1 with FP4 weights: intensity $\approx 2$ FLOP/byte. Still deep in the memory-bound regime. Even Blackwell's enormous FP4 compute advantage does not change this: at batch size 1, the weight load dominates regardless of precision. The 8\,TB/s bandwidth is what matters, not the 18,000 TFLOP/s.

\textbf{The RL tension.} Quantization makes rollouts cheaper but noisier. The critical question for RLVR is not ``does quantization degrade perplexity?'' --- it is ``does quantization degrade the reward signal enough to slow policy learning?'' These are different measurements. RL amplifies small distributional shifts: a quantized model that generates slightly different token distributions produces different reward statistics, which produce different advantage estimates, which produce different gradients. A 2\% perplexity degradation might be invisible in supervised evaluation but produce measurably higher gradient variance in RLVR.

The practical protocol: run a short RL training ablation (100--500 gradient steps) comparing FP16 and quantized rollout generation. Compare reward curves, gradient norm variance, and KL divergence from the reference policy. If gradient variance increases by $>20\%$ under quantization, the cheaper rollouts are costing you convergence speed.

\begin{thinkfirst}[Quantization Acceptability for RL]
If you quantize model weights from FP16 to INT4 and rollout quality degrades by 2\% on an eval benchmark, is this acceptable for RL training? What would you need to measure to answer this question?

The eval benchmark measures generation quality on a fixed distribution. RL training cares about the reward signal's informativeness for policy improvement. Consider:
\begin{itemize}
  \item Does the 2\% quality drop change which rollouts receive high vs.\ low rewards? If the reward model assigns similar scores to FP16 and INT4 outputs, the advantage estimates are similar and quantization is fine.
  \item Does the distributional shift from quantization interact with the reward function? For binary rewards (correct/incorrect on math problems), a small quality drop may flip some borderline problems from correct to incorrect --- changing the reward signal discontinuously.
  \item Does the gradient variance increase? Even if mean reward is similar, higher variance in the advantage estimates slows convergence.
  \item Does the KL divergence between the quantized policy and the reference policy differ from the FP16 case? GRPO penalizes KL divergence; quantization-induced distributional shift adds an uncontrolled source of KL that may interact with the KL penalty.
\end{itemize}

The answer is: you cannot determine acceptability from an eval benchmark alone. You need to measure reward variance, gradient norm statistics, and convergence speed directly under RL training conditions.
\end{thinkfirst}

% -----------------------------------------------------------------------
\subsubsection*{2c: MoE Routing and Expert Kernels}
% -----------------------------------------------------------------------

Mixture-of-Experts models replace the dense FFN with $E$ parallel expert FFNs, routing each token to the top-$k$ experts via a learned gating function. This introduces kernel-level challenges that do not exist in dense models.

\textbf{The forward pass.} At each MoE layer, the router network (a small linear layer) computes gating scores for all $E$ experts, selects the top-$k$ per token, dispatches tokens to the selected experts, computes the expert FFN outputs, and scatters results back. For a batch of $B$ tokens:

\begin{enumerate}
  \item \textbf{Route:} compute $[B \times E]$ gating scores, select top-$k$ per row. Compute-cheap, memory-cheap.
  \item \textbf{Dispatch:} gather tokens into per-expert batches. Each expert receives a variable number of tokens. This is an irregular scatter operation.
  \item \textbf{Compute:} run $k$ expert FFNs. Each expert's batch size varies --- some experts may receive many tokens, others few or none.
  \item \textbf{Combine:} scatter expert outputs back to the original token positions and combine (weighted sum by gating scores).
\end{enumerate}

\textbf{Load balancing.} If the router consistently sends tokens to the same subset of experts, unused experts waste SRAM and compute. A model with 8 experts and 2 active per token but 80\% of tokens routed to experts 0 and 1 is effectively a dense model with 6 dead expert FFNs occupying HBM. The auxiliary load-balancing loss encourages uniform routing, but under RLVR training, the hidden state distribution shifts with each policy update, creating transient load imbalances that the loss has not yet corrected.

\textbf{Expert parallelism.} When experts are distributed across GPUs (each GPU holds $E / \text{num\_gpus}$ experts), every MoE layer requires all-to-all communication: tokens must be sent to the GPU holding their selected expert, processed, and sent back. For a model with $L/2$ MoE layers (alternating dense and MoE), this is $L$ all-to-all operations per autoregressive step. At large batch sizes, this communication can dominate step latency.

\textbf{Sparse dispatch kernels.} The gather-compute-scatter pattern is challenging to implement efficiently. Naive implementations use \texttt{torch.index\_select} for gathering and \texttt{scatter\_add} for combining --- each a separate kernel launch with its own DRAM round-trip. Expert batch sizes are irregular: the GEMM for an expert receiving 3 tokens is a very different shape from one receiving 300 tokens. Padding to a uniform expert batch size wastes compute; dynamic-shape GEMMs underutilize Tensor Cores at small sizes.

\textbf{Roofline position.} MoE forward passes have lower \textit{effective} arithmetic intensity than their theoretical intensity suggests. The theoretical intensity accounts for $k$ expert FFNs, each performing standard GEMMs. The effective intensity is reduced by: dispatch overhead (irregular memory accesses for token gathering), load imbalance (idle experts waste allocated compute), and communication overhead (all-to-all for expert parallelism). On the roofline diagram, MoE operations plot below and to the left of where a dense model with the same active parameter count would sit.

% =======================================================================
\subsection*{Move 3: Cross-Platform Pattern Matching}
% =======================================================================

The optimization principles from Move~2 apply to every accelerator. The specific APIs and hardware abstractions differ, but the diagnostic thinking is identical: find the bottleneck on the roofline, optimize data movement, overlap compute and memory access. This section maps the concepts between GPU (CUDA) and TPU (Pallas/XLA) to make the universality concrete.

\textbf{GPU: threads and memory access patterns.} On a GPU, thousands of lightweight threads execute the same instruction on different data (SIMT). The programmer manages \textit{shared memory} --- fast on-chip SRAM visible to all threads in a thread block. The optimization loop:
\begin{enumerate}
  \item Tile the computation so each thread block's working set fits in shared memory.
  \item Load tiles from HBM to shared memory with coalesced accesses (consecutive threads access consecutive addresses).
  \item Compute on the tile, reusing data from shared memory.
  \item Use warp-level primitives (\texttt{mma.sync}, shuffle instructions) to coordinate within a 32-thread warp without shared memory round-trips.
\end{enumerate}

\textbf{TPU: pipeline efficiency and keeping the systolic array fed.} On a TPU, a 128$\times$128 systolic array (MXU) flows data through in a deterministic pattern. There is no warp scheduling randomness. The programmer manages \textit{VMEM} --- on-chip SRAM ($\sim$30\,MB on v5e) --- and \textit{DMA transfers} between HBM and VMEM. The optimization loop:
\begin{enumerate}
  \item Tile the computation so each tile's inputs fit in VMEM.
  \item Prefetch the next tile via DMA while the current tile is being computed on the MXU.
  \item Match tile dimensions to the MXU size (128$\times$128). Undersized tiles underutilize the systolic array.
  \item Use double-buffering (\texttt{Buffered(buffer\_count=2)} in Pallas) to overlap DMA and compute.
\end{enumerate}

\textbf{The mapping.} The concepts are the same; only the vocabulary changes:

\begin{center}
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{GPU (CUDA)} & \textbf{TPU (Pallas/XLA)} \\
\midrule
Shared memory tiling & VMEM prefetching \\
Warp-level primitives (\texttt{mma.sync}) & Sublane operations on MXU \\
Thread blocks & Tiles (grid cells) \\
Double-buffering in shared memory & \texttt{Buffered(buffer\_count=2)} DMA \\
Bank conflict avoidance & VMEM access pattern alignment \\
Tensor Core utilization & MXU utilization \\
\texttt{\_\_syncthreads()} & Implicit tile synchronization \\
\bottomrule
\end{tabular}
\end{center}

\medskip

\textbf{DMA/compute overlap: the pipeline timeline.} On both platforms, the highest-performance kernels overlap data movement with computation. While the arithmetic units process tile~$N$, the memory system loads tile~$N{+}1$. Throughput per tile is $\max(\text{compute time}, \text{load time})$, not their sum.

\begin{center}
\begin{tikzpicture}[
  x=1.2cm, y=0.8cm,
  phase/.style={draw, thick, minimum height=0.6cm, text centered, font=\scriptsize},
  dma/.style={phase, fill=aiiro!15, rounded corners=2pt},
  compute/.style={phase, fill=kitsune!15, rounded corners=2pt},
  store/.style={phase, fill=matcha!10, rounded corners=2pt}
]
  % Labels
  \node[font=\footnotesize\bfseries, anchor=east] at (-0.2, 2) {DMA / Load};
  \node[font=\footnotesize\bfseries, anchor=east] at (-0.2, 1) {Compute};
  \node[font=\footnotesize\bfseries, anchor=east] at (-0.2, 0) {Store};

  % Time axis
  \draw[-{Stealth[length=4pt]}, sumi!50] (0,-0.6) -- (9.5,-0.6)
    node[right, font=\scriptsize] {time};

  % DMA row
  \node[dma, minimum width=1.6cm] at (0.9, 2) {Tile 0};
  \node[dma, minimum width=1.6cm] at (2.7, 2) {Tile 1};
  \node[dma, minimum width=1.6cm] at (4.5, 2) {Tile 2};
  \node[dma, minimum width=1.6cm] at (6.3, 2) {Tile 3};

  % Compute row (offset by one tile)
  \node[compute, minimum width=1.6cm] at (2.7, 1) {Tile 0};
  \node[compute, minimum width=1.6cm] at (4.5, 1) {Tile 1};
  \node[compute, minimum width=1.6cm] at (6.3, 1) {Tile 2};
  \node[compute, minimum width=1.6cm] at (8.1, 1) {Tile 3};

  % Store row (offset by two tiles)
  \node[store, minimum width=1.6cm] at (4.5, 0) {Tile 0};
  \node[store, minimum width=1.6cm] at (6.3, 0) {Tile 1};
  \node[store, minimum width=1.6cm] at (8.1, 0) {Tile 2};

  % Overlap brace
  \draw[decorate, decoration={brace, amplitude=4pt, mirror}, thick, sumi!40]
    (1.9, -0.15) -- (3.5, -0.15)
    node[midway, below=5pt, font=\scriptsize\itshape, sumi!60] {overlap};

  % Annotation
  \node[font=\scriptsize\itshape, sumi!50, text width=3cm, align=center]
    at (4.5, -1.3) {Throughput = max(load, compute, store) per tile};
\end{tikzpicture}
\end{center}

\smallskip
{\small\textit{DMA/compute/store pipeline. While tile $N$ is computed, tile $N+1$ is loaded and tile $N-1$ is stored. On a GPU, this is double-buffering in shared memory. On a TPU, this is Pallas's \texttt{Buffered} prefetching. The optimization thinking is identical; only the API differs.}}

\medskip

\textbf{Memory hierarchy mapping.}

\begin{center}
\begin{tikzpicture}[
  mem/.style={draw, thick, rounded corners=3pt, minimum width=2.8cm, minimum height=0.7cm,
              text centered, font=\small},
  lbl/.style={font=\scriptsize\itshape, text width=2.2cm, align=center},
  arr/.style={-{Stealth[length=4pt]}, thick, sumi!50}
]
  % GPU side
  \node[font=\footnotesize\bfseries] at (1.4, 3.5) {GPU (CUDA)};
  \node[mem, fill=beni!8]    (greg) at (1.4, 2.8) {Registers};
  \node[mem, fill=kitsune!8] (gsmem) at (1.4, 1.8) {Shared Mem};
  \node[mem, fill=aiiro!8]   (ghbm) at (1.4, 0.8) {HBM};
  \draw[arr] (ghbm) -- (gsmem);
  \draw[arr] (gsmem) -- (greg);

  % TPU side
  \node[font=\footnotesize\bfseries] at (5.6, 3.5) {TPU (Pallas)};
  \node[mem, fill=beni!8]    (treg) at (5.6, 2.8) {VREGs};
  \node[mem, fill=kitsune!8] (tvmem) at (5.6, 1.8) {VMEM};
  \node[mem, fill=aiiro!8]   (thbm) at (5.6, 0.8) {HBM};
  \draw[arr] (thbm) -- node[right, font=\scriptsize, sumi!60] {DMA} (tvmem);
  \draw[arr] (tvmem) -- (treg);

  % Equivalence arrows
  \draw[sumi!30, dashed, thick, {Stealth[length=4pt]}-{Stealth[length=4pt]}]
    (greg.east) -- (treg.west);
  \draw[sumi!30, dashed, thick, {Stealth[length=4pt]}-{Stealth[length=4pt]}]
    (gsmem.east) -- (tvmem.west);
  \draw[sumi!30, dashed, thick, {Stealth[length=4pt]}-{Stealth[length=4pt]}]
    (ghbm.east) -- (thbm.west);

  % Equivalence label
  \node[font=\scriptsize\itshape, sumi!50] at (3.5, 1.8) {same role};
\end{tikzpicture}
\end{center}

\textbf{Key message.} The optimization \textit{thinking} is identical across platforms: diagnose the bottleneck with the roofline, optimize data movement to reduce bytes transferred across the memory wall, overlap compute and memory access to hide latency. Only the API changes. A practitioner who understands the principles on one platform can transfer to another in days, not months. The roofline model is the portable skill.

\begin{exercise}[Cross-Platform Attention Optimization]
Take the FlashAttention optimization from Move~2a (tile QKV computation to fit in SRAM, use online softmax to avoid materializing the $N^2$ matrix) and describe how you would implement the same optimization on a TPU using Pallas. Focus on the conceptual mapping, not the syntax.

Address these questions:
\begin{itemize}
  \item FlashAttention tiles $Q$ into blocks that fit in shared memory. On a TPU, what determines the tile size? (VMEM capacity and MXU dimensions.)
  \item FlashAttention uses \texttt{\_\_syncthreads()} between loading a KV tile and computing attention scores. What is the TPU equivalent? (Implicit in Pallas's grid execution model --- each tile is a separate grid cell.)
  \item FlashAttention double-buffers KV tiles: loading tile $k+1$ while computing on tile $k$. How does this map to Pallas? (\texttt{Buffered(buffer\_count=2)} in the \texttt{BlockSpec}.)
  \item The online softmax maintains running $(m, \ell)$ state across tiles. On a TPU, where does this state live? (VMEM scratch space via \texttt{scratch\_shapes}, or VREGs if small enough.)
  \item FlashAttention's backward pass recomputes the forward attention from saved $Q$, $K$, $V$ instead of storing the $N^2$ matrix. Does this recomputation strategy change on a TPU? (No --- the memory-vs-recompute tradeoff is hardware-independent; the TPU's larger VMEM may allow slightly larger tiles but the algorithm is the same.)
\end{itemize}

Write a one-paragraph summary: what is shared between the GPU and TPU implementations, and what must change?
\end{exercise}

\newpage
